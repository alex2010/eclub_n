{{#if title}}
    <{{or h 'h3'}} class="head">
    <strong>{{title}}</strong>
    </{{or h 'h3'}}>
{{/if}}
<{{or tag 'div'}} class="refresh"></{{or tag 'div'}}>

{{#if foot}}
    <div class="foot"></div>
{{/if}}